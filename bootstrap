#!/usr/bin/env zsh

STOW_FOLDERS="zsh,nvim,git,curl,brew,starship,ghostty,zellij"

stow -t ~ stow

for folder in $(echo $STOW_FOLDERS | sed "s/,/ /g")
do
    echo "Stowing $folder"
    stow --restow $folder
done

echo "Done."
