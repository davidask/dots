#!/usr/bin/env zsh

echo "Brewing..."
brew bundle --file ~/.Brewfile --quiet --zap

eval $(brew --prefix)/opt/fzf/install \
  --no-bash \
  --no-fish \
  --key-bindings \
  --completion \
  --no-update-rc
